lergo-build:
  cached: false
  # cached: false # commenting out for development. should not be commented when live
  build:
    image: lergo-build
    dockerfile: Dockerfile
    args:
      this_is_lergopro_pem  //to pass into Dockerfile	
  encrypted_env_file: env.encrypted
